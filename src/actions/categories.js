import {
  FETCH_CATEGORIES,
  FETCH_CATEGORIES_RECIEVED,
  FETCH_CATEGORIES_REJECTED
} from './types';

export function getCategories() {
  //fetch the categories once they are stored on the server
};
